routes:
collections:
  /:
    permalink: /post/{slug}
    limit: all
    filter: primary_tag:tag-1
  /team:
    permalink: /team/{slug}
    template: team
    limit: all
    filter: primary_tag:tag-2
taxonomies:
  tag: /tags/{slug}/
  author: /authors/{slug}/
