import * as React from 'react'
import type { PageProps } from 'gatsby'
import { View } from '@gspenst/components'

const IndexPage = (props: PageProps) => {
  return <View>@gspenst/gatsby-starter-typescript</View>
}

export default IndexPage
